{
  "name": "nexus-platform",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter @nexus/web dev",
    "dev:web": "pnpm --filter @nexus/web dev",
    "dev:api": "pnpm --filter @nexus/api dev",
    "dev:all": "turbo dev",
    "build": "pnpm --filter @nexus/web build",
    "build:web": "pnpm --filter @nexus/web build",
    "build:api": "pnpm --filter @nexus/api build",
    "build:all": "turbo build",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc --noEmit --project apps/web/tsconfig.json && tsc --noEmit --project apps/api/tsconfig.json",
    "test": "npm --prefix apps/api run test",
    "db:migrate:local": "pnpm --filter @nexus/api wrangler d1 migrations apply meridian-nexus-db --local --config wrangler.jsonc",
    "db:migrate:remote": "pnpm --filter @nexus/api wrangler d1 migrations apply meridian-nexus-db --remote --config wrangler.jsonc",
    "db:seed:local": "pnpm --filter @nexus/api wrangler d1 execute meridian-nexus-db --local --file=seed/seed.sql --config wrangler.jsonc",
    "db:seed:remote": "pnpm --filter @nexus/api wrangler d1 execute meridian-nexus-db --remote --file=seed/seed.sql --config wrangler.jsonc",
    "deploy:api": "pnpm --filter @nexus/api deploy",
    "clean": "rm -rf node_modules apps/web/node_modules apps/api/node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0",
    "prettier": "^3.4.0",
    "eslint": "^8.57.0",
    "@typescript-eslint/parser": "^6.21.0",
    "@typescript-eslint/eslint-plugin": "^6.21.0"
  }
}
